<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.fabulous">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.send_mail.SendMailActivity" />

        <activity android:name=".ui.editor.Editor" />
        <activity android:name=".ui.blog.BlogDetailActivity" />
        <activity android:name=".ui.habit_detail.HabitDetailActivity" />
        <activity android:name=".ui.blog.BlogActivity" />
        <activity android:name=".ui.exercise.ExerciseActivity" />
        <!--        <activity android:name=".ui.user.ProfileActivity"></activity>-->
        <activity android:name=".ui.habit_category.HabitCategoryActivity" />
        <!--        <activity android:name=".ui.user.ProfileActivity"></activity>-->

        <activity android:name=".ui.todos.CreateTodoActivity"></activity>
        <activity android:name=".ui.focusMode.FocusModeActivity" />
        <activity android:name=".ui.user.ProfileActivity" />

        <!--        <activity android:name=".ui.statistic.StatisticActivity" />-->
        <!--        <activity android:name=".ui.journal.JournalActivity" />-->
        <activity android:name=".ui.home.HomeActivity" />
        <activity android:name=".ui.user.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>